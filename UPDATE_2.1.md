# HNSIC 2.1 更新公告

## 版本说明

`2.1` 是在 `beta2.0` 基础上的继续整理版本，这次重点不再只是单点修 bug，而是把 `AI 分组 -> 模式投票 -> 地图选择 -> 开赛` 这条比赛主链继续补顺，同时把前面已经做过的权限、菜单、皮肤、点位和账号系统修复整合进同一版本说明里。

这次版本的目标很明确：

- 让正式比赛流程更清楚
- 让 AI 分组后的队伍归属真正保存下来
- 让模式选择和地图选择更符合比赛服使用习惯
- 继续提升整套插件在实际开服时的稳定性

## 本次重点更新

### AI 分组结果正式保存

- AI 分组完成后，会保存每个玩家当前所属队伍
- 会记录谁在 `A 队`，谁在 `B 队`
- 会同步写入比赛主系统使用的 `playerslist`
- 后续进入比赛流程、换图、继续开赛时，主系统也能识别原始分队结果
- 管理员使用交换队伍后，新的队伍归属也会重新保存

这次修完以后，AI 分组不再只是“当场分一下”，而是正式进入后续比赛链路可读取的状态。

### 比赛模式逻辑修正

`2.1` 里继续明确了正式比赛模式的范围。

当前比赛模式按正式逻辑保留：

- `MR`
- `Time`
- `单挑积分`
- `回合制`
- `点位积分`
- `点位扣除`

其中：

- `回合制` 现在明确算正式比赛模式
- 不再被错误排除在比赛主流程之外

### 人数限制修正

模式投票现在按人数做区分：

- `2 人` 时：
  - 显示 `MR / Time / 单挑积分 / 回合制 / 点位积分 / 点位扣除`
- `大于 2 人` 时：
  - 隐藏 `单挑积分`
  - 保留 `MR / Time / 回合制 / 点位积分 / 点位扣除`

也就是说，现在多人局只会去掉单挑，不会再把 `回合制` 一起漏掉。

### AI 分组后的比赛主流程继续整理

当前主流程继续朝下面这个方向整理：

1. `AI 分组完成`
2. `投票选择比赛模式`
3. `选择随机地图或指定地图`
4. `切换到目标地图`
5. `开始比赛`

这样比赛逻辑比之前更顺，不会再让 AI 分组结果和真正开赛之间出现断层。

### 地图选择逻辑继续收口

当前地图逻辑按模式继续约束：

- `MR / Time`
  - 随机地图时可进入正式比赛图池
- `回合制`
  - 保留为正式比赛模式参与后续流程
- `点位积分 / 点位扣除`
  - 继续按对应玩法地图逻辑处理
- `单挑积分`
  - 仅在 `2 人` 情况下参与投票和后续流程

### 继承 beta2.0 的核心修复

`2.1` 同时包含 `beta2.0` 阶段已经完成的核心整理内容：

- 权限系统修复
- 聊天前缀修复
- 官方管理员菜单修复
- 中文化补充
- 点位积分系统判定修复
- 皮肤系统整理
- 内测账号注册 / 登录系统
- AI 分组重连恢复、替补、状态恢复优化

## 已包含的重要功能整理

### 权限与管理员菜单

- 修复服主和管理员无法打开官方 `AMX Mod X` 管理员菜单的问题
- 管理员输入 `/menu` 时优先打开官方管理员菜单
- 普通玩家继续使用原有玩家菜单
- 服务器语言改为中文
- 补充官方菜单相关语言文件

### 点位积分

- 修复默认点位判定框过大
- `3/4/5人点` 使用更合理范围
- 占点改成包围盒判定
- 正式接入停留时间判定
- 修复 `Vampire` 点位任务清理问题

### 皮肤系统

- 默认皮肤自动发放逻辑保留
- 额外皮肤按单个发放处理
- 支持未解锁皮肤显示
- 增加内测账号绑定逻辑

### 内测账号系统

支持以下命令：

- `/reg <内测名> <密码>`
- `/login <内测名> <密码>`
- `/logout`
- `/account`

当前逻辑：

- 已登录内测账号时，皮肤优先绑定到账号
- 未登录时，继续兼容旧识别方式
- 减少盗版环境下因改名、冒名导致的皮肤数据混乱

## 当前版本状态

`2.1` 目前属于继续整合与测试版本。  
代码已经推进到可测试状态，重点进入服内实测阶段。

建议优先测试：

- AI 分组后队伍归属是否正确保存
- 换图后原队伍是否能正确恢复
- 多人模式投票里是否正确保留 `回合制`
- 多人模式投票里是否正确隐藏 `单挑积分`
- 随机地图 / 指定地图流程是否符合预期
- 比赛开始前后的模式、地图、队伍是否一致

## 总结

`2.1` 的核心不是单独新增某个炫目的功能，而是把整套比赛系统再往“可正式使用”推进一步。

这次最关键的变化有两点：

- `AI 分组结果正式进入比赛主系统`
- `回合制重新纳入正式比赛模式链路`

后续如果测试中继续发现边界问题，会在后面的版本里继续补齐。
