# HNSIC 2.2 更新公告

## 版本说明

`2.2` 这次不是加新玩法，而是继续做菜单和比赛流程清理。  
重点就是把那些会让人误会成“重复系统”的入口收掉，尤其是点位积分相关入口，避免普通比赛菜单和点位编辑工具混在一起。

这次版本的核心目标很直接：

- 清理重复或容易误解的入口
- 让普通比赛菜单只保留比赛相关内容
- 让点位编辑工具不再混进普通比赛链路
- 把 `2.1` 里已经做好的 AI 分组保存继续接进更清晰的菜单结构

## 本次重点更新

### 1. 清理点位积分重复入口

这次重点收掉了点位相关的重复感。

现在的逻辑改成：

- `比赛模式里的点位积分 / 点位扣除`
  - 仍然保留在正式比赛模式里
- `点位编辑器`
  - 不再放在普通比赛快捷入口里误导使用
- `点位分数配置`
  - 只有进入点位模式时才显示和允许调整

这样就不会再出现：

- 普通比赛菜单里看见点位模式
- 外面又看见一个点位编辑器入口
- 让人误以为有两套点位系统同时在跑

### 2. 比赛设置菜单收口

`比赛设置` 这次做了进一步收口：

- 非点位模式下：
  - 不再把点位目标分数和点位分数配置当成常驻比赛设置项
- 只有当前处于：
  - `点位积分模式`
  - `点位扣除模式`

才会显示：

- 目标分数
- 点位分数配置

也就是说，`MR / Time / 回合制 / 普通比赛` 这些模式下，不会再看到不属于它们的点位设置。

### 3. 快捷操作菜单整理

普通比赛菜单里的 `快捷操作` 继续收口：

- 去掉了把 `点位编辑器` 直接摆在快捷操作里的做法
- 避免普通比赛中途误点到地图点位编辑工具
- 普通比赛菜单现在更偏向真正的比赛控制，而不是地图制作工具

### 4. 延续 2.1 的 AI 分组保存逻辑

`2.2` 同时继承并保留了 `2.1` 已经完成的关键逻辑：

- AI 分组完成后保存每个玩家所属队伍
- 同步写入比赛主系统使用的 `playerslist`
- 换图后主系统也能识别谁在 T，谁在 CT
- `swap` 后会重新保存队伍归属

这部分没有回退，而是继续作为正式比赛链的一部分保留。

### 5. 正式比赛模式继续维持统一

这次版本继续保持前一轮已经修正好的模式逻辑：

- `MR`
- `Time`
- `单挑积分`
- `回合制`
- `点位积分`
- `点位扣除`

并且继续保持：

- `回合制` 作为正式比赛模式保留
- 多人局只隐藏 `单挑积分`
- 多人局仍保留 `回合制`

## 这次版本解决的实际问题

`2.2` 主要解决的是使用体验上的混乱，而不是底层玩法本身。

以前容易出现的感觉是：

- 比赛菜单里已经有点位积分系统
- 外面又还有点位编辑器
- 设置里又一直挂着点位配置

最后看起来像：

- 有两套点位系统
- 或者一套正式比赛系统外面又套了个点位插件

这次整理后，逻辑更清楚了：

- 比赛菜单负责比赛
- 点位模式只在进入点位比赛时显示对应配置
- 编辑器不再挂在普通比赛快捷入口里

## 当前建议测试

建议你这次重点测试这几项：

- 普通 `MR / Time / 回合制` 下打开比赛设置
  - 看看还会不会常驻显示点位配置
- 进入 `点位积分 / 点位扣除` 模式后
  - 是否正常显示目标分数和点位分数配置
- 快捷操作菜单
  - 是否已经不再把点位编辑器放在普通比赛入口里
- AI 分组后进入比赛流程
  - 队伍归属是否仍然正常保存

## 总结

`2.2` 的本质是一次菜单和入口整理版本。

它没有去新增花哨功能，而是把原来容易造成误解的结构继续收干净，尤其是点位积分相关入口。

如果说：

- `2.1` 是把 AI 分组和比赛保存链路接起来

那：

- `2.2` 就是把比赛菜单和点位工具的边界重新划清楚
