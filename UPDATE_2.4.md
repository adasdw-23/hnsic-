# HNSIC 2.4 更新说明

## 本轮重点

这次先落地的是 `点位积分系统比赛规则层第一阶段`，重点不是继续修编辑器，而是把点位积分从“谁进点谁固定加分”的旧逻辑，往正式比赛规则推进一层。

## 已完成

### 1. 动态主点规则骨架

- 增加了 `动态主点观察窗口`
- 增加了 `主点额外分`
- 这两个参数都已接入比赛设置中的点位配置菜单
- 开局会在观察窗口内跟踪实际主攻点位
- 观察窗口内会累计“主攻强度趋势”，窗口结束后再判定主点
- 如果本回合出现明显冲突或无法判定，就取消主点额外加分（只提示一次，避免刷屏）

### 2. 占点不再是瞬间结算

- `Ascension` 现在不再是“只要 T 进点立刻给分”
- 已改为按 `停留时间` 判定
- 达到停留时间后才算一次有效占点
- 同一点位同回合仍只结算一次

### 2.1 综合积分接入（第二阶段开始）

- 新增 `击杀加分`
- 新增 `生存加分（回合结算一次）`
- 默认数值偏小，避免击杀/生存压过占点
- 两个参数都已经接入比赛设置，可动态调整

### 3. 主点加分进入正式结算

- 如果有效占到的是当前主点
- 会在原始点位分基础上再加 `主点额外分`
- HUD 会持续显示当前主点状态

### 4. 平回合提示与主点重置骨架

- 如果这一回合没有任何有效占点
- 现在会明确提示为 `平回合`
- 回合结束时会重置主点状态，下一回合重新判断

### 5. 无点位配置禁止开赛

- `Ascension`
- `Vampire`

这两个点位模式现在都会先检查地图是否已配置点位。  
没有配置时会直接拦截，不再硬开。

### 6. 点位编辑器与命令修复

- 查看/删除点位菜单支持分页，不再只显示前 9 个
- 删除点位不再把后面的标签强行改名，避免 A/B/C 乱掉
- `/delzone` 支持：`/delzone <A-Z> [3|4|5]`，可按标签或按标签+类型精确删除
- `/creatzone` 支持：`/creatzone [3|4|5]` 或 `/creatzone <A-Z> [3|4|5]`，并会自动找空闲标签

### 7. 点位模式随机图过滤

- 点位积分/点位扣除模式下，随机地图将只从“已配置点位”的地图中抽取

## 已接入比赛设置的参数

点位菜单里现在能调：

- `3人分数`
- `4人分数`
- `5人分数`
- `停留时间`
- `主点观察窗口`
- `主点额外分`
- `击杀加分`
- `生存加分（每存活1人）`

## 这一轮还没做完的

这次属于 `第一阶段`，还没把你想要的大更新全部做满。  
后面还要继续补的重点有：

- 综合积分的完整规则
- 动态主点冲突时更细的判断
- 更完整的 HUD / 聊天提示去重
- 点位积分模式和地图选择流程更深联动
- 点位地图配置开赛前的完整校验

## 当前交付

这次交付的是：

- 已更新源码树
- 已补 `2.4` 说明
- 可继续在这份源码上往下做下一阶段
