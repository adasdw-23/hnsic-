# HNSIC 2.3 更新公告

## 版本说明

`2.3` 这次不主打新功能，重点是做一次真正的源码与插件瘦身。  
目标很明确：把已经废弃、重复、冲突、或者根本不再参与当前运行的旧插件源码清掉，但保留对外接口和兼容层，避免后续维护时继续被旧文件误导。

这次版本可以概括成一句话：

- 删掉没用的旧插件和旧 `sma`
- 保留还需要的 `include / native / bridge` 接口

## 本次重点更新

### 清理废弃插件源码

`2.3` 已删除以下已经废弃或被替代的旧源码：

- `HnsMatchPointScap.sma`
- `HnsMatchSkin.sma`
- `HnsMatchAdminSkins.sma`
- `HnsMatchPlayerModels.sma`
- `HnsMatchModeHud.sma`
- `HnsMatchSemiClip.sma`
- `hnsic/HnsIC.sma`

这些文件的共同特点是：

- 当前 `plugins.ini` 并未加载
- 功能已经被核心系统或新插件取代
- 或者属于旧版大杂烩逻辑，继续留着只会干扰维护判断

### 保留接口兼容

这次清理不是粗暴删空，而是保留了还需要的接口层：

- 相关 `include` 文件继续保留
- `native` 声明继续保留
- 旧桥接文件继续保留
- 当前核心插件需要的兼容入口继续保留

也就是说，`2.3` 删除的是旧实现，不是把兼容能力一起砍掉。

### 清理目标说明

这次删除的文件分别对应这些旧角色：

- `HnsMatchPointScap.sma`
  - 点位编辑和点位模式功能已经内置到核心系统
- `HnsMatchSkin.sma`
  - 已被 `HnsMatchSkinSystem` 完整替代
- `HnsMatchAdminSkins.sma`
  - 功能已并入皮肤系统
- `HnsMatchPlayerModels.sma`
  - 功能已并入皮肤系统
- `HnsMatchModeHud.sma`
  - HUD 已内置，继续保留旧源码没有意义
- `HnsMatchSemiClip.sma`
  - 当前不再走独立插件实现
- `hnsic/HnsIC.sma`
  - 旧版大杂烩主插件，和当前核心系统路线冲突

本轮清理里，`HnsMatchStatsMysql.sma` 已按后续修正保留源码，不再作为删除项处理。  
当前是否启用它，继续由你的实际运行配置决定。

## 本次同时整理的内容

除了删旧源码，这次还顺手把一些误导性说明改掉了：

- 更新了 `plugins.ini` 里的说明注释
- 把 `SemiClip` 相关的旧注释改成当前真实逻辑
- 避免以后再看到“交给另一个旧 amxx 处理”的误导说法

## 2.3 之后的结构变化

现在仓库里的结构会更接近当前真实运行状态：

- 运行中使用的核心插件源码保留
- 已废弃的旧实现源码移除
- 接口文件和兼容桥保留

这样后面再看仓库时，判断会清楚很多：

- 什么是当前正式实现
- 什么是兼容层
- 什么已经完全不用了

## 当前版本状态

`2.3` 现在更像一次“清库版本”。  
不是增加玩法，而是把代码仓库整理到更贴近当前实际运行状态的样子。

## 总结

如果说：

- `2.1` 重点是把 AI 分组结果真正接进比赛主流程
- `2.2` 重点是清理点位积分菜单和重复入口

那么：

- `2.3` 重点就是清理旧插件、旧 `sma` 和历史包袱

这次版本完成后，当前插件体系会更轻、更清楚，也更方便继续往后做 `2.4` 甚至更高版本。
