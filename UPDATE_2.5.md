# HNSIC 2.5 更新公告

## 版本定位

`2.5` 是基于 `2.4 点位积分系统大更新` 的发布版补丁，重点不是再改一堆逻辑，而是把「可直接上服用的插件补丁」和「可直接丢进服务器的完整版」一次性做出来。

你可以把 `2.5` 理解成：

- `2.4` 负责把点位积分系统大重做
- `2.5` 负责把这一套整理成可交付的插件包（含编译产物）

## 本次交付内容

### 1. 最新插件补丁（Patch）

包含：

- `configs/plugins.ini` 里启用的所有 `.amxx` 插件编译产物
- 适合你直接覆盖服务器 `cstrike/addons/amxmodx/plugins/` 使用

### 2. 完整版（Full）

包含：

- 完整的 `cstrike/` 目录结构
- `configs/` 配置文件
- `plugins/` 已编译好的插件
- `scripting/` 源码（方便你后续继续改/二次编译）

## 2.4 规则回顾（核心变化点）

`2.4` 的点位积分系统大更新包括：

- 动态主点（观察窗口 + 趋势判定/锁定）
- 占点改为停留时间判定，不再“进点瞬间加分”
- 主点额外分（比赛设置可调）
- 综合积分接入（击杀加分 + 生存加分，保持占点主轴）
- 无点位配置地图直接拦截点位模式开赛
- 点位模式随机图过滤（只抽有点位配置的地图）
- 点位编辑器分页、删除不乱标签、命令更精确

## 说明

`2.5` 主要目标是“能直接用”，所以本次重点是把编译产物和完整目录一次性交付出来，避免你每次更新还得手动凑插件。
